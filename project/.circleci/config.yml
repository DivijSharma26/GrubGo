version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:10.16.3
    steps:
      - checkout
      - run: html index.html

workflows:
  version: 2
  build_now:
    jobs:
      - build:
          